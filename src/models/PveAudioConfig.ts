/* tslint:disable */
/* eslint-disable */
/**
 * Proxmox Virtual Environment API
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { PveQemuDeviceEnum } from './PveQemuDeviceEnum';
import {
    PveQemuDeviceEnumFromJSON,
    PveQemuDeviceEnumFromJSONTyped,
    PveQemuDeviceEnumToJSON,
    PveQemuDeviceEnumToJSONTyped,
} from './PveQemuDeviceEnum';
import type { PveDriverEnum } from './PveDriverEnum';
import {
    PveDriverEnumFromJSON,
    PveDriverEnumFromJSONTyped,
    PveDriverEnumToJSON,
    PveDriverEnumToJSONTyped,
} from './PveDriverEnum';

/**
 * 
 * @export
 * @interface PveAudioConfig
 */
export interface PveAudioConfig {

    /**
     * Configure an audio device.
     * @type {PveQemuDeviceEnum}
     * @memberof PveAudioConfig
     */
    device: PveQemuDeviceEnum;

    /**
     * Driver backend for the audio device.
     * @type {PveDriverEnum}
     * @memberof PveAudioConfig
     */
    driver?: PveDriverEnum;

}



/**
 * Check if a given object implements the PveAudioConfig interface.
 */
export function instanceOfPveAudioConfig(value: object): value is PveAudioConfig {
    
    if (!('device' in value) || value['device'] === undefined) return false;
    
    
    return true;
}

export function PveAudioConfigFromJSON(json: any): PveAudioConfig {
    return PveAudioConfigFromJSONTyped(json, false);
}

export function PveAudioConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): PveAudioConfig {
    if (json == null) {
        return json;
    }
    return {
        
        
        'device': PveQemuDeviceEnumFromJSON(json['device']),
        
        'driver': json['driver'] == null ? undefined : PveDriverEnumFromJSON(json['driver']),
        
    };
}

export function PveAudioConfigToJSON(json: any): PveAudioConfig {
    return PveAudioConfigToJSONTyped(json, false);
}

export function PveAudioConfigToJSONTyped(value?: PveAudioConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        
        'device': PveQemuDeviceEnumToJSON(value['device']),
        
        'driver': PveDriverEnumToJSON(value['driver']),
        
    };
}

/**
 * Serialise a PveAudioConfig into Proxmox's CLI-style shorthand string
 * (`key=value,…`). The property marked `x-pve-default-key` is emitted
 * positionally without a `key=` prefix; aliases collapse multiple
 * property names to the same wire key.
 *
 * Example: `{ model: 'virtio', bridge: 'vmbr0', firewall: 1 }`
 *      → `'virtio,bridge=vmbr0,firewall=1'`
 */
export function PveAudioConfigToShorthand(value?: PveAudioConfig | null): string | undefined {
    if (value == null) return undefined;
    const parts: string[] = [];
    
    
    if (value['device'] != null) {
        parts.push(`device=${String(value['device'])}`);
    }
    
    if (value['driver'] != null) {
        parts.push(`driver=${String(value['driver'])}`);
    }
    
    return parts.join(',');
}

/**
 * Parse a Proxmox shorthand string into a PveAudioConfig. Mirror of
 * PveAudioConfigToShorthand. The first comma-separated segment without
 * `=` is interpreted as the default-key value.
 */
export function PveAudioConfigFromShorthand(input?: string | null): PveAudioConfig | undefined {
    if (input == null) return undefined;
    const out: any = {};
    let defaultKeyConsumed = false;
    for (const part of input.split(',')) {
        if (!part) continue;
        const eq = part.indexOf('=');
        if (eq < 0) {
            
            continue;
        }
        const k = part.slice(0, eq);
        const v = part.slice(eq + 1);
        out[k] = v;
    }
    return out as PveAudioConfig;
}

