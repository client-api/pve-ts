/* tslint:disable */
/* eslint-disable */
/**
 * Proxmox Virtual Environment API
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { PveClusterOptionsUserAllowEnum } from './PveClusterOptionsUserAllowEnum';
import {
    PveClusterOptionsUserAllowEnumFromJSON,
    PveClusterOptionsUserAllowEnumFromJSONTyped,
    PveClusterOptionsUserAllowEnumToJSON,
    PveClusterOptionsUserAllowEnumToJSONTyped,
} from './PveClusterOptionsUserAllowEnum';

/**
 * 
 * @export
 * @interface PveUserTagAccessConfig
 */
export interface PveUserTagAccessConfig {

    /**
     * Controls which tags can be set or deleted on resources a user controls (such as guests). Users with the `Sys.Modify` privilege on `/` are alwaysunrestricted.
     * * 'none' no tags are usable.
     * * 'list' tags from 'user-allow-list' are usable.
     * * 'existing' like list, but already existing tags of resources are also usable.
     * * 'free' no tag restrictions.
     * 
     * @type {PveClusterOptionsUserAllowEnum}
     * @memberof PveUserTagAccessConfig
     */
    userAllow?: PveClusterOptionsUserAllowEnum;

    /**
     * List of tags users are allowed to set and delete (semicolon separated) for 'user-allow' values 'list' and 'existing'.
     * @type {string}
     * @memberof PveUserTagAccessConfig
     */
    userAllowList?: string;

}



/**
 * Check if a given object implements the PveUserTagAccessConfig interface.
 */
export function instanceOfPveUserTagAccessConfig(value: object): value is PveUserTagAccessConfig {
    
    
    
    return true;
}

export function PveUserTagAccessConfigFromJSON(json: any): PveUserTagAccessConfig {
    return PveUserTagAccessConfigFromJSONTyped(json, false);
}

export function PveUserTagAccessConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): PveUserTagAccessConfig {
    if (json == null) {
        return json;
    }
    return {
        
        
        'userAllow': json['user-allow'] == null ? undefined : PveClusterOptionsUserAllowEnumFromJSON(json['user-allow']),
        
        'userAllowList': json['user-allow-list'] == null ? undefined : json['user-allow-list'],
        
    };
}

export function PveUserTagAccessConfigToJSON(json: any): PveUserTagAccessConfig {
    return PveUserTagAccessConfigToJSONTyped(json, false);
}

export function PveUserTagAccessConfigToJSONTyped(value?: PveUserTagAccessConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        
        'user-allow': PveClusterOptionsUserAllowEnumToJSON(value['userAllow']),
        
        'user-allow-list': value['userAllowList'],
        
    };
}

/**
 * Serialise a PveUserTagAccessConfig into Proxmox's CLI-style shorthand string
 * (`key=value,…`). The property marked `x-pve-default-key` is emitted
 * positionally without a `key=` prefix; aliases collapse multiple
 * property names to the same wire key.
 *
 * Example: `{ model: 'virtio', bridge: 'vmbr0', firewall: 1 }`
 *      → `'virtio,bridge=vmbr0,firewall=1'`
 */
export function PveUserTagAccessConfigToShorthand(value?: PveUserTagAccessConfig | null): string | undefined {
    if (value == null) return undefined;
    const parts: string[] = [];
    
    
    if (value['userAllow'] != null) {
        parts.push(`user-allow=${String(value['userAllow'])}`);
    }
    
    if (value['userAllowList'] != null) {
        parts.push(`user-allow-list=${String(value['userAllowList'])}`);
    }
    
    return parts.join(',');
}

/**
 * Parse a Proxmox shorthand string into a PveUserTagAccessConfig. Mirror of
 * PveUserTagAccessConfigToShorthand. The first comma-separated segment without
 * `=` is interpreted as the default-key value.
 */
export function PveUserTagAccessConfigFromShorthand(input?: string | null): PveUserTagAccessConfig | undefined {
    if (input == null) return undefined;
    const out: any = {};
    let defaultKeyConsumed = false;
    for (const part of input.split(',')) {
        if (!part) continue;
        const eq = part.indexOf('=');
        if (eq < 0) {
            
            continue;
        }
        const k = part.slice(0, eq);
        const v = part.slice(eq + 1);
        out[k] = v;
    }
    return out as PveUserTagAccessConfig;
}

