/* tslint:disable */
/* eslint-disable */
/**
 * Proxmox Virtual Environment API
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { PveBoolean } from './PveBoolean';
import {
    PveBooleanFromJSON,
    PveBooleanFromJSONTyped,
    PveBooleanToJSON,
    PveBooleanToJSONTyped,
} from './PveBoolean';

/**
 * 
 * @export
 * @interface PveWebauthnConfig
 */
export interface PveWebauthnConfig {

    /**
     * Whether to allow the origin to be a subdomain, rather than the exact URL.
     * @type {PveBoolean}
     * @memberof PveWebauthnConfig
     */
    allowSubdomains?: PveBoolean;

    /**
     * Relying party ID. Must be the domain name without protocol, port or location. Changing this *will* break existing credentials.
     * @type {string}
     * @memberof PveWebauthnConfig
     */
    id?: string;

    /**
     * Site origin. Must be a `https://` URL (or `http://localhost`). Should contain the address users type in their browsers to access the web interface. Changing this *may* break existing credentials.
     * @type {string}
     * @memberof PveWebauthnConfig
     */
    origin?: string;

    /**
     * Relying party name. Any text identifier. Changing this *may* break existing credentials.
     * @type {string}
     * @memberof PveWebauthnConfig
     */
    rp?: string;

}



/**
 * Check if a given object implements the PveWebauthnConfig interface.
 */
export function instanceOfPveWebauthnConfig(value: object): value is PveWebauthnConfig {
    
    
    
    
    
    return true;
}

export function PveWebauthnConfigFromJSON(json: any): PveWebauthnConfig {
    return PveWebauthnConfigFromJSONTyped(json, false);
}

export function PveWebauthnConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): PveWebauthnConfig {
    if (json == null) {
        return json;
    }
    return {
        
        
        'allowSubdomains': json['allow-subdomains'] == null ? undefined : PveBooleanFromJSON(json['allow-subdomains']),
        
        'id': json['id'] == null ? undefined : json['id'],
        
        'origin': json['origin'] == null ? undefined : json['origin'],
        
        'rp': json['rp'] == null ? undefined : json['rp'],
        
    };
}

export function PveWebauthnConfigToJSON(json: any): PveWebauthnConfig {
    return PveWebauthnConfigToJSONTyped(json, false);
}

export function PveWebauthnConfigToJSONTyped(value?: PveWebauthnConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        
        'allow-subdomains': PveBooleanToJSON(value['allowSubdomains']),
        
        'id': value['id'],
        
        'origin': value['origin'],
        
        'rp': value['rp'],
        
    };
}

/**
 * Serialise a PveWebauthnConfig into Proxmox's CLI-style shorthand string
 * (`key=value,…`). The property marked `x-pve-default-key` is emitted
 * positionally without a `key=` prefix; aliases collapse multiple
 * property names to the same wire key.
 *
 * Example: `{ model: 'virtio', bridge: 'vmbr0', firewall: 1 }`
 *      → `'virtio,bridge=vmbr0,firewall=1'`
 */
export function PveWebauthnConfigToShorthand(value?: PveWebauthnConfig | null): string | undefined {
    if (value == null) return undefined;
    const parts: string[] = [];
    
    
    if (value['allowSubdomains'] != null) {
        parts.push(`allow-subdomains=${String(value['allowSubdomains'])}`);
    }
    
    if (value['id'] != null) {
        parts.push(`id=${String(value['id'])}`);
    }
    
    if (value['origin'] != null) {
        parts.push(`origin=${String(value['origin'])}`);
    }
    
    if (value['rp'] != null) {
        parts.push(`rp=${String(value['rp'])}`);
    }
    
    return parts.join(',');
}

/**
 * Parse a Proxmox shorthand string into a PveWebauthnConfig. Mirror of
 * PveWebauthnConfigToShorthand. The first comma-separated segment without
 * `=` is interpreted as the default-key value.
 */
export function PveWebauthnConfigFromShorthand(input?: string | null): PveWebauthnConfig | undefined {
    if (input == null) return undefined;
    const out: any = {};
    let defaultKeyConsumed = false;
    for (const part of input.split(',')) {
        if (!part) continue;
        const eq = part.indexOf('=');
        if (eq < 0) {
            
            continue;
        }
        const k = part.slice(0, eq);
        const v = part.slice(eq + 1);
        out[k] = v;
    }
    return out as PveWebauthnConfig;
}

