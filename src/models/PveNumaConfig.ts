/* tslint:disable */
/* eslint-disable */
/**
 * Proxmox Virtual Environment API
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { PvePolicyEnum } from './PvePolicyEnum';
import {
    PvePolicyEnumFromJSON,
    PvePolicyEnumFromJSONTyped,
    PvePolicyEnumToJSON,
    PvePolicyEnumToJSONTyped,
} from './PvePolicyEnum';

/**
 * 
 * @export
 * @interface PveNumaConfig
 */
export interface PveNumaConfig {

    /**
     * CPUs accessing this NUMA node.
     * @type {string}
     * @memberof PveNumaConfig
     */
    cpus: string;

    /**
     * Host NUMA nodes to use.
     * @type {string}
     * @memberof PveNumaConfig
     */
    hostnodes?: string;

    /**
     * Amount of memory this NUMA node provides.
     * @type {number}
     * @memberof PveNumaConfig
     */
    memory?: number;

    /**
     * NUMA allocation policy.
     * @type {PvePolicyEnum}
     * @memberof PveNumaConfig
     */
    policy?: PvePolicyEnum;

}



/**
 * Check if a given object implements the PveNumaConfig interface.
 */
export function instanceOfPveNumaConfig(value: object): value is PveNumaConfig {
    
    if (!('cpus' in value) || value['cpus'] === undefined) return false;
    
    
    
    
    return true;
}

export function PveNumaConfigFromJSON(json: any): PveNumaConfig {
    return PveNumaConfigFromJSONTyped(json, false);
}

export function PveNumaConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): PveNumaConfig {
    if (json == null) {
        return json;
    }
    return {
        
        
        'cpus': json['cpus'],
        
        'hostnodes': json['hostnodes'] == null ? undefined : json['hostnodes'],
        
        'memory': json['memory'] == null ? undefined : json['memory'],
        
        'policy': json['policy'] == null ? undefined : PvePolicyEnumFromJSON(json['policy']),
        
    };
}

export function PveNumaConfigToJSON(json: any): PveNumaConfig {
    return PveNumaConfigToJSONTyped(json, false);
}

export function PveNumaConfigToJSONTyped(value?: PveNumaConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        
        'cpus': value['cpus'],
        
        'hostnodes': value['hostnodes'],
        
        'memory': value['memory'],
        
        'policy': PvePolicyEnumToJSON(value['policy']),
        
    };
}

/**
 * Serialise a PveNumaConfig into Proxmox's CLI-style shorthand string
 * (`key=value,…`). The property marked `x-pve-default-key` is emitted
 * positionally without a `key=` prefix; aliases collapse multiple
 * property names to the same wire key.
 *
 * Example: `{ model: 'virtio', bridge: 'vmbr0', firewall: 1 }`
 *      → `'virtio,bridge=vmbr0,firewall=1'`
 */
export function PveNumaConfigToShorthand(value?: PveNumaConfig | null): string | undefined {
    if (value == null) return undefined;
    const parts: string[] = [];
    
    
    if (value['cpus'] != null) {
        parts.push(`cpus=${String(value['cpus'])}`);
    }
    
    if (value['hostnodes'] != null) {
        parts.push(`hostnodes=${String(value['hostnodes'])}`);
    }
    
    if (value['memory'] != null) {
        parts.push(`memory=${String(value['memory'])}`);
    }
    
    if (value['policy'] != null) {
        parts.push(`policy=${String(value['policy'])}`);
    }
    
    return parts.join(',');
}

/**
 * Parse a Proxmox shorthand string into a PveNumaConfig. Mirror of
 * PveNumaConfigToShorthand. The first comma-separated segment without
 * `=` is interpreted as the default-key value.
 */
export function PveNumaConfigFromShorthand(input?: string | null): PveNumaConfig | undefined {
    if (input == null) return undefined;
    const out: any = {};
    let defaultKeyConsumed = false;
    for (const part of input.split(',')) {
        if (!part) continue;
        const eq = part.indexOf('=');
        if (eq < 0) {
            
            continue;
        }
        const k = part.slice(0, eq);
        const v = part.slice(eq + 1);
        out[k] = v;
    }
    return out as PveNumaConfig;
}

