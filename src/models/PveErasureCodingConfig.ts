/* tslint:disable */
/* eslint-disable */
/**
 * Proxmox Virtual Environment API
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * 
 * @export
 * @interface PveErasureCodingConfig
 */
export interface PveErasureCodingConfig {

    /**
     * CRUSH device class. Will create an erasure coded pool plus a replicated pool for metadata.
     * @type {string}
     * @memberof PveErasureCodingConfig
     */
    deviceClass?: string;

    /**
     * CRUSH failure domain. Default is 'host'. Will create an erasure coded pool plus a replicated pool for metadata.
     * @type {string}
     * @memberof PveErasureCodingConfig
     */
    failureDomain?: string;

    /**
     * Number of data chunks. Will create an erasure coded pool plus a replicated pool for metadata.
     * @type {number}
     * @memberof PveErasureCodingConfig
     */
    k: number;

    /**
     * Number of coding chunks. Will create an erasure coded pool plus a replicated pool for metadata.
     * @type {number}
     * @memberof PveErasureCodingConfig
     */
    m: number;

    /**
     * Override the erasure code (EC) profile to use. Will create an erasure coded pool plus a replicated pool for metadata.
     * @type {string}
     * @memberof PveErasureCodingConfig
     */
    profile?: string;

}

/**
 * Check if a given object implements the PveErasureCodingConfig interface.
 */
export function instanceOfPveErasureCodingConfig(value: object): value is PveErasureCodingConfig {
    
    
    
    if (!('k' in value) || value['k'] === undefined) return false;
    
    if (!('m' in value) || value['m'] === undefined) return false;
    
    
    return true;
}

export function PveErasureCodingConfigFromJSON(json: any): PveErasureCodingConfig {
    return PveErasureCodingConfigFromJSONTyped(json, false);
}

export function PveErasureCodingConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): PveErasureCodingConfig {
    if (json == null) {
        return json;
    }
    return {
        
        
        'deviceClass': json['device-class'] == null ? undefined : json['device-class'],
        
        'failureDomain': json['failure-domain'] == null ? undefined : json['failure-domain'],
        
        'k': json['k'],
        
        'm': json['m'],
        
        'profile': json['profile'] == null ? undefined : json['profile'],
        
    };
}

export function PveErasureCodingConfigToJSON(json: any): PveErasureCodingConfig {
    return PveErasureCodingConfigToJSONTyped(json, false);
}

export function PveErasureCodingConfigToJSONTyped(value?: PveErasureCodingConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        
        'device-class': value['deviceClass'],
        
        'failure-domain': value['failureDomain'],
        
        'k': value['k'],
        
        'm': value['m'],
        
        'profile': value['profile'],
        
    };
}

/**
 * Serialise a PveErasureCodingConfig into Proxmox's CLI-style shorthand string
 * (`key=value,…`). The property marked `x-pve-default-key` is emitted
 * positionally without a `key=` prefix; aliases collapse multiple
 * property names to the same wire key.
 *
 * Example: `{ model: 'virtio', bridge: 'vmbr0', firewall: 1 }`
 *      → `'virtio,bridge=vmbr0,firewall=1'`
 */
export function PveErasureCodingConfigToShorthand(value?: PveErasureCodingConfig | null): string | undefined {
    if (value == null) return undefined;
    const parts: string[] = [];
    
    
    if (value['deviceClass'] != null) {
        parts.push(`device-class=${String(value['deviceClass'])}`);
    }
    
    if (value['failureDomain'] != null) {
        parts.push(`failure-domain=${String(value['failureDomain'])}`);
    }
    
    if (value['k'] != null) {
        parts.push(`k=${String(value['k'])}`);
    }
    
    if (value['m'] != null) {
        parts.push(`m=${String(value['m'])}`);
    }
    
    if (value['profile'] != null) {
        parts.push(`profile=${String(value['profile'])}`);
    }
    
    return parts.join(',');
}

/**
 * Parse a Proxmox shorthand string into a PveErasureCodingConfig. Mirror of
 * PveErasureCodingConfigToShorthand. The first comma-separated segment without
 * `=` is interpreted as the default-key value.
 */
export function PveErasureCodingConfigFromShorthand(input?: string | null): PveErasureCodingConfig | undefined {
    if (input == null) return undefined;
    const out: any = {};
    let defaultKeyConsumed = false;
    for (const part of input.split(',')) {
        if (!part) continue;
        const eq = part.indexOf('=');
        if (eq < 0) {
            
            continue;
        }
        const k = part.slice(0, eq);
        const v = part.slice(eq + 1);
        out[k] = v;
    }
    return out as PveErasureCodingConfig;
}

