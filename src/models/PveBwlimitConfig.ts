/* tslint:disable */
/* eslint-disable */
/**
 * Proxmox Virtual Environment API
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * 
 * @export
 * @interface PveBwlimitConfig
 */
export interface PveBwlimitConfig {

    /**
     * bandwidth limit in KiB/s for cloning disks
     * @type {number}
     * @memberof PveBwlimitConfig
     */
    clone?: number;

    /**
     * default bandwidth limit in KiB/s
     * @type {number}
     * @memberof PveBwlimitConfig
     */
    _default?: number;

    /**
     * bandwidth limit in KiB/s for migrating guests (including moving local disks)
     * @type {number}
     * @memberof PveBwlimitConfig
     */
    migration?: number;

    /**
     * bandwidth limit in KiB/s for moving disks
     * @type {number}
     * @memberof PveBwlimitConfig
     */
    move?: number;

    /**
     * bandwidth limit in KiB/s for restoring guests from backups
     * @type {number}
     * @memberof PveBwlimitConfig
     */
    restore?: number;

}

/**
 * Check if a given object implements the PveBwlimitConfig interface.
 */
export function instanceOfPveBwlimitConfig(value: object): value is PveBwlimitConfig {
    
    
    
    
    
    
    return true;
}

export function PveBwlimitConfigFromJSON(json: any): PveBwlimitConfig {
    return PveBwlimitConfigFromJSONTyped(json, false);
}

export function PveBwlimitConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): PveBwlimitConfig {
    if (json == null) {
        return json;
    }
    return {
        
        
        'clone': json['clone'] == null ? undefined : json['clone'],
        
        '_default': json['default'] == null ? undefined : json['default'],
        
        'migration': json['migration'] == null ? undefined : json['migration'],
        
        'move': json['move'] == null ? undefined : json['move'],
        
        'restore': json['restore'] == null ? undefined : json['restore'],
        
    };
}

export function PveBwlimitConfigToJSON(json: any): PveBwlimitConfig {
    return PveBwlimitConfigToJSONTyped(json, false);
}

export function PveBwlimitConfigToJSONTyped(value?: PveBwlimitConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        
        'clone': value['clone'],
        
        'default': value['_default'],
        
        'migration': value['migration'],
        
        'move': value['move'],
        
        'restore': value['restore'],
        
    };
}

/**
 * Serialise a PveBwlimitConfig into Proxmox's CLI-style shorthand string
 * (`key=value,…`). The property marked `x-pve-default-key` is emitted
 * positionally without a `key=` prefix; aliases collapse multiple
 * property names to the same wire key.
 *
 * Example: `{ model: 'virtio', bridge: 'vmbr0', firewall: 1 }`
 *      → `'virtio,bridge=vmbr0,firewall=1'`
 */
export function PveBwlimitConfigToShorthand(value?: PveBwlimitConfig | null): string | undefined {
    if (value == null) return undefined;
    const parts: string[] = [];
    
    
    if (value['clone'] != null) {
        parts.push(`clone=${String(value['clone'])}`);
    }
    
    if (value['_default'] != null) {
        parts.push(`default=${String(value['_default'])}`);
    }
    
    if (value['migration'] != null) {
        parts.push(`migration=${String(value['migration'])}`);
    }
    
    if (value['move'] != null) {
        parts.push(`move=${String(value['move'])}`);
    }
    
    if (value['restore'] != null) {
        parts.push(`restore=${String(value['restore'])}`);
    }
    
    return parts.join(',');
}

/**
 * Parse a Proxmox shorthand string into a PveBwlimitConfig. Mirror of
 * PveBwlimitConfigToShorthand. The first comma-separated segment without
 * `=` is interpreted as the default-key value.
 */
export function PveBwlimitConfigFromShorthand(input?: string | null): PveBwlimitConfig | undefined {
    if (input == null) return undefined;
    const out: any = {};
    let defaultKeyConsumed = false;
    for (const part of input.split(',')) {
        if (!part) continue;
        const eq = part.indexOf('=');
        if (eq < 0) {
            
            continue;
        }
        const k = part.slice(0, eq);
        const v = part.slice(eq + 1);
        out[k] = v;
    }
    return out as PveBwlimitConfig;
}

