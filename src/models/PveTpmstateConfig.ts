/* tslint:disable */
/* eslint-disable */
/**
 * Proxmox Virtual Environment API
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { PveQemuFormatEnum } from './PveQemuFormatEnum';
import {
    PveQemuFormatEnumFromJSON,
    PveQemuFormatEnumFromJSONTyped,
    PveQemuFormatEnumToJSON,
    PveQemuFormatEnumToJSONTyped,
} from './PveQemuFormatEnum';
import type { PveVersionEnum } from './PveVersionEnum';
import {
    PveVersionEnumFromJSON,
    PveVersionEnumFromJSONTyped,
    PveVersionEnumToJSON,
    PveVersionEnumToJSONTyped,
} from './PveVersionEnum';

/**
 * 
 * @export
 * @interface PveTpmstateConfig
 */
export interface PveTpmstateConfig {

    /**
     * The drive's backing volume.
     * @type {string}
     * @memberof PveTpmstateConfig
     */
    file: string;

    /**
     * Format of the image.
     * @type {PveQemuFormatEnum}
     * @memberof PveTpmstateConfig
     */
    format?: PveQemuFormatEnum;

    /**
     * Create a new disk, importing from this source (volume ID or absolute path). When an absolute path is specified, it's up to you to ensure that the source is not actively used by another process during the import!
     * @type {string}
     * @memberof PveTpmstateConfig
     */
    importFrom?: string;

    /**
     * Disk size. This is purely informational and has no effect.
     * @type {string}
     * @memberof PveTpmstateConfig
     */
    size?: string;

    /**
     * The TPM interface version. v2.0 is newer and should be preferred. Note that this cannot be changed later on.
     * @type {PveVersionEnum}
     * @memberof PveTpmstateConfig
     */
    version?: PveVersionEnum;

    /**
     * 
     * @type {string}
     * @memberof PveTpmstateConfig
     */
    volume?: string;

}



/**
 * Check if a given object implements the PveTpmstateConfig interface.
 */
export function instanceOfPveTpmstateConfig(value: object): value is PveTpmstateConfig {
    
    if (!('file' in value) || value['file'] === undefined) return false;
    
    
    
    
    
    
    return true;
}

export function PveTpmstateConfigFromJSON(json: any): PveTpmstateConfig {
    return PveTpmstateConfigFromJSONTyped(json, false);
}

export function PveTpmstateConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): PveTpmstateConfig {
    if (json == null) {
        return json;
    }
    return {
        
        
        'file': json['file'],
        
        'format': json['format'] == null ? undefined : PveQemuFormatEnumFromJSON(json['format']),
        
        'importFrom': json['import-from'] == null ? undefined : json['import-from'],
        
        'size': json['size'] == null ? undefined : json['size'],
        
        'version': json['version'] == null ? undefined : PveVersionEnumFromJSON(json['version']),
        
        'volume': json['volume'] == null ? undefined : json['volume'],
        
    };
}

export function PveTpmstateConfigToJSON(json: any): PveTpmstateConfig {
    return PveTpmstateConfigToJSONTyped(json, false);
}

export function PveTpmstateConfigToJSONTyped(value?: PveTpmstateConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        
        'file': value['file'],
        
        'format': PveQemuFormatEnumToJSON(value['format']),
        
        'import-from': value['importFrom'],
        
        'size': value['size'],
        
        'version': PveVersionEnumToJSON(value['version']),
        
        'volume': value['volume'],
        
    };
}

/**
 * Serialise a PveTpmstateConfig into Proxmox's CLI-style shorthand string
 * (`key=value,…`). The property marked `x-pve-default-key` is emitted
 * positionally without a `key=` prefix; aliases collapse multiple
 * property names to the same wire key.
 *
 * Example: `{ model: 'virtio', bridge: 'vmbr0', firewall: 1 }`
 *      → `'virtio,bridge=vmbr0,firewall=1'`
 */
export function PveTpmstateConfigToShorthand(value?: PveTpmstateConfig | null): string | undefined {
    if (value == null) return undefined;
    const parts: string[] = [];
    
    if (value['file'] != null) {
        parts.push(String(value['file']));
    }
    
    
    if (value['format'] != null) {
        parts.push(`format=${String(value['format'])}`);
    }
    
    if (value['importFrom'] != null) {
        parts.push(`import-from=${String(value['importFrom'])}`);
    }
    
    if (value['size'] != null) {
        parts.push(`size=${String(value['size'])}`);
    }
    
    if (value['version'] != null) {
        parts.push(`version=${String(value['version'])}`);
    }
    
    if (value['volume'] != null) {
        parts.push(`file=${String(value['volume'])}`);
    }
    
    return parts.join(',');
}

/**
 * Parse a Proxmox shorthand string into a PveTpmstateConfig. Mirror of
 * PveTpmstateConfigToShorthand. The first comma-separated segment without
 * `=` is interpreted as the default-key value.
 */
export function PveTpmstateConfigFromShorthand(input?: string | null): PveTpmstateConfig | undefined {
    if (input == null) return undefined;
    const out: any = {};
    let defaultKeyConsumed = false;
    for (const part of input.split(',')) {
        if (!part) continue;
        const eq = part.indexOf('=');
        if (eq < 0) {
            
            if (!defaultKeyConsumed) {
                out['file'] = part;
                defaultKeyConsumed = true;
                continue;
            }
            
            continue;
        }
        const k = part.slice(0, eq);
        const v = part.slice(eq + 1);
        out[k] = v;
    }
    return out as PveTpmstateConfig;
}

