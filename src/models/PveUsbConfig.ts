/* tslint:disable */
/* eslint-disable */
/**
 * Proxmox Virtual Environment API
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { PveBoolean } from './PveBoolean';
import {
    PveBooleanFromJSON,
    PveBooleanFromJSONTyped,
    PveBooleanToJSON,
    PveBooleanToJSONTyped,
} from './PveBoolean';

/**
 * 
 * @export
 * @interface PveUsbConfig
 */
export interface PveUsbConfig {

    /**
     * The Host USB device or port or the value 'spice'. HOSTUSBDEVICE syntax is:
     * 
     *  'bus-port(.port)*' (decimal numbers) or
     *  'vendor_id:product_id' (hexadecimal numbers) or
     *  'spice'
     * 
     * You can use the 'lsusb -t' command to list existing usb devices.
     * 
     * NOTE: This option allows direct access to host hardware. So it is no longer possible to migrate such
     * machines - use with special care.
     * 
     * The value 'spice' can be used to add a usb redirection devices for spice.
     * 
     * Either this or the 'mapping' key must be set.
     * 
     * @type {string}
     * @memberof PveUsbConfig
     */
    host?: string;

    /**
     * The ID of a cluster wide mapping. Either this or the default-key 'host' must be set.
     * @type {string}
     * @memberof PveUsbConfig
     */
    mapping?: string;

    /**
     * Specifies whether if given host option is a USB3 device or port. For modern guests (machine version >= 7.1 and ostype l26 and windows > 7), this flag is irrelevant (all devices are plugged into a xhci controller).
     * @type {PveBoolean}
     * @memberof PveUsbConfig
     */
    usb3?: PveBoolean;

}



/**
 * Check if a given object implements the PveUsbConfig interface.
 */
export function instanceOfPveUsbConfig(value: object): value is PveUsbConfig {
    
    
    
    
    return true;
}

export function PveUsbConfigFromJSON(json: any): PveUsbConfig {
    return PveUsbConfigFromJSONTyped(json, false);
}

export function PveUsbConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): PveUsbConfig {
    if (json == null) {
        return json;
    }
    return {
        
        
        'host': json['host'] == null ? undefined : json['host'],
        
        'mapping': json['mapping'] == null ? undefined : json['mapping'],
        
        'usb3': json['usb3'] == null ? undefined : PveBooleanFromJSON(json['usb3']),
        
    };
}

export function PveUsbConfigToJSON(json: any): PveUsbConfig {
    return PveUsbConfigToJSONTyped(json, false);
}

export function PveUsbConfigToJSONTyped(value?: PveUsbConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        
        'host': value['host'],
        
        'mapping': value['mapping'],
        
        'usb3': PveBooleanToJSON(value['usb3']),
        
    };
}

/**
 * Serialise a PveUsbConfig into Proxmox's CLI-style shorthand string
 * (`key=value,…`). The property marked `x-pve-default-key` is emitted
 * positionally without a `key=` prefix; aliases collapse multiple
 * property names to the same wire key.
 *
 * Example: `{ model: 'virtio', bridge: 'vmbr0', firewall: 1 }`
 *      → `'virtio,bridge=vmbr0,firewall=1'`
 */
export function PveUsbConfigToShorthand(value?: PveUsbConfig | null): string | undefined {
    if (value == null) return undefined;
    const parts: string[] = [];
    
    if (value['host'] != null) {
        parts.push(String(value['host']));
    }
    
    
    if (value['mapping'] != null) {
        parts.push(`mapping=${String(value['mapping'])}`);
    }
    
    if (value['usb3'] != null) {
        parts.push(`usb3=${String(value['usb3'])}`);
    }
    
    return parts.join(',');
}

/**
 * Parse a Proxmox shorthand string into a PveUsbConfig. Mirror of
 * PveUsbConfigToShorthand. The first comma-separated segment without
 * `=` is interpreted as the default-key value.
 */
export function PveUsbConfigFromShorthand(input?: string | null): PveUsbConfig | undefined {
    if (input == null) return undefined;
    const out: any = {};
    let defaultKeyConsumed = false;
    for (const part of input.split(',')) {
        if (!part) continue;
        const eq = part.indexOf('=');
        if (eq < 0) {
            
            if (!defaultKeyConsumed) {
                out['host'] = part;
                defaultKeyConsumed = true;
                continue;
            }
            
            continue;
        }
        const k = part.slice(0, eq);
        const v = part.slice(eq + 1);
        out[k] = v;
    }
    return out as PveUsbConfig;
}

