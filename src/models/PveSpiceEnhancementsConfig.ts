/* tslint:disable */
/* eslint-disable */
/**
 * Proxmox Virtual Environment API
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { PveBoolean } from './PveBoolean';
import {
    PveBooleanFromJSON,
    PveBooleanFromJSONTyped,
    PveBooleanToJSON,
    PveBooleanToJSONTyped,
} from './PveBoolean';
import type { PveVideostreamingEnum } from './PveVideostreamingEnum';
import {
    PveVideostreamingEnumFromJSON,
    PveVideostreamingEnumFromJSONTyped,
    PveVideostreamingEnumToJSON,
    PveVideostreamingEnumToJSONTyped,
} from './PveVideostreamingEnum';

/**
 * 
 * @export
 * @interface PveSpiceEnhancementsConfig
 */
export interface PveSpiceEnhancementsConfig {

    /**
     * Enable folder sharing via SPICE. Needs Spice-WebDAV daemon installed in the VM.
     * @type {PveBoolean}
     * @memberof PveSpiceEnhancementsConfig
     */
    foldersharing?: PveBoolean;

    /**
     * Enable video streaming. Uses compression for detected video streams.
     * @type {PveVideostreamingEnum}
     * @memberof PveSpiceEnhancementsConfig
     */
    videostreaming?: PveVideostreamingEnum;

}



/**
 * Check if a given object implements the PveSpiceEnhancementsConfig interface.
 */
export function instanceOfPveSpiceEnhancementsConfig(value: object): value is PveSpiceEnhancementsConfig {
    
    
    
    return true;
}

export function PveSpiceEnhancementsConfigFromJSON(json: any): PveSpiceEnhancementsConfig {
    return PveSpiceEnhancementsConfigFromJSONTyped(json, false);
}

export function PveSpiceEnhancementsConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): PveSpiceEnhancementsConfig {
    if (json == null) {
        return json;
    }
    return {
        
        
        'foldersharing': json['foldersharing'] == null ? undefined : PveBooleanFromJSON(json['foldersharing']),
        
        'videostreaming': json['videostreaming'] == null ? undefined : PveVideostreamingEnumFromJSON(json['videostreaming']),
        
    };
}

export function PveSpiceEnhancementsConfigToJSON(json: any): PveSpiceEnhancementsConfig {
    return PveSpiceEnhancementsConfigToJSONTyped(json, false);
}

export function PveSpiceEnhancementsConfigToJSONTyped(value?: PveSpiceEnhancementsConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        
        'foldersharing': PveBooleanToJSON(value['foldersharing']),
        
        'videostreaming': PveVideostreamingEnumToJSON(value['videostreaming']),
        
    };
}

/**
 * Serialise a PveSpiceEnhancementsConfig into Proxmox's CLI-style shorthand string
 * (`key=value,…`). The property marked `x-pve-default-key` is emitted
 * positionally without a `key=` prefix; aliases collapse multiple
 * property names to the same wire key.
 *
 * Example: `{ model: 'virtio', bridge: 'vmbr0', firewall: 1 }`
 *      → `'virtio,bridge=vmbr0,firewall=1'`
 */
export function PveSpiceEnhancementsConfigToShorthand(value?: PveSpiceEnhancementsConfig | null): string | undefined {
    if (value == null) return undefined;
    const parts: string[] = [];
    
    
    if (value['foldersharing'] != null) {
        parts.push(`foldersharing=${String(value['foldersharing'])}`);
    }
    
    if (value['videostreaming'] != null) {
        parts.push(`videostreaming=${String(value['videostreaming'])}`);
    }
    
    return parts.join(',');
}

/**
 * Parse a Proxmox shorthand string into a PveSpiceEnhancementsConfig. Mirror of
 * PveSpiceEnhancementsConfigToShorthand. The first comma-separated segment without
 * `=` is interpreted as the default-key value.
 */
export function PveSpiceEnhancementsConfigFromShorthand(input?: string | null): PveSpiceEnhancementsConfig | undefined {
    if (input == null) return undefined;
    const out: any = {};
    let defaultKeyConsumed = false;
    for (const part of input.split(',')) {
        if (!part) continue;
        const eq = part.indexOf('=');
        if (eq < 0) {
            
            continue;
        }
        const k = part.slice(0, eq);
        const v = part.slice(eq + 1);
        out[k] = v;
    }
    return out as PveSpiceEnhancementsConfig;
}

