/* tslint:disable */
/* eslint-disable */
/**
 * Proxmox Virtual Environment API
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * 
 * @export
 * @interface PveLinkConfig
 */
export interface PveLinkConfig {

    /**
     * Hostname (or IP) of this corosync link address.
     * @type {string}
     * @memberof PveLinkConfig
     */
    address: string;

    /**
     * The priority for the link when knet is used in 'passive' mode (default). Lower value means higher priority. Only valid for cluster create, ignored on node add.
     * @type {number}
     * @memberof PveLinkConfig
     */
    priority?: number;

}

/**
 * Check if a given object implements the PveLinkConfig interface.
 */
export function instanceOfPveLinkConfig(value: object): value is PveLinkConfig {
    
    if (!('address' in value) || value['address'] === undefined) return false;
    
    
    return true;
}

export function PveLinkConfigFromJSON(json: any): PveLinkConfig {
    return PveLinkConfigFromJSONTyped(json, false);
}

export function PveLinkConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): PveLinkConfig {
    if (json == null) {
        return json;
    }
    return {
        
        
        'address': json['address'],
        
        'priority': json['priority'] == null ? undefined : json['priority'],
        
    };
}

export function PveLinkConfigToJSON(json: any): PveLinkConfig {
    return PveLinkConfigToJSONTyped(json, false);
}

export function PveLinkConfigToJSONTyped(value?: PveLinkConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        
        'address': value['address'],
        
        'priority': value['priority'],
        
    };
}

/**
 * Serialise a PveLinkConfig into Proxmox's CLI-style shorthand string
 * (`key=value,…`). The property marked `x-pve-default-key` is emitted
 * positionally without a `key=` prefix; aliases collapse multiple
 * property names to the same wire key.
 *
 * Example: `{ model: 'virtio', bridge: 'vmbr0', firewall: 1 }`
 *      → `'virtio,bridge=vmbr0,firewall=1'`
 */
export function PveLinkConfigToShorthand(value?: PveLinkConfig | null): string | undefined {
    if (value == null) return undefined;
    const parts: string[] = [];
    
    if (value['address'] != null) {
        parts.push(String(value['address']));
    }
    
    
    if (value['priority'] != null) {
        parts.push(`priority=${String(value['priority'])}`);
    }
    
    return parts.join(',');
}

/**
 * Parse a Proxmox shorthand string into a PveLinkConfig. Mirror of
 * PveLinkConfigToShorthand. The first comma-separated segment without
 * `=` is interpreted as the default-key value.
 */
export function PveLinkConfigFromShorthand(input?: string | null): PveLinkConfig | undefined {
    if (input == null) return undefined;
    const out: any = {};
    let defaultKeyConsumed = false;
    for (const part of input.split(',')) {
        if (!part) continue;
        const eq = part.indexOf('=');
        if (eq < 0) {
            
            if (!defaultKeyConsumed) {
                out['address'] = part;
                defaultKeyConsumed = true;
                continue;
            }
            
            continue;
        }
        const k = part.slice(0, eq);
        const v = part.slice(eq + 1);
        out[k] = v;
    }
    return out as PveLinkConfig;
}

