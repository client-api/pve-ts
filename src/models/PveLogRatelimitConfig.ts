/* tslint:disable */
/* eslint-disable */
/**
 * Proxmox Virtual Environment API
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { PveBoolean } from './PveBoolean';
import {
    PveBooleanFromJSON,
    PveBooleanFromJSONTyped,
    PveBooleanToJSON,
    PveBooleanToJSONTyped,
} from './PveBoolean';

/**
 * 
 * @export
 * @interface PveLogRatelimitConfig
 */
export interface PveLogRatelimitConfig {

    /**
     * Initial burst of packages which will always get logged before the rate is applied
     * @type {number}
     * @memberof PveLogRatelimitConfig
     */
    burst?: number;

    /**
     * Enable or disable log rate limiting
     * @type {PveBoolean}
     * @memberof PveLogRatelimitConfig
     */
    enable: PveBoolean;

    /**
     * Frequency with which the burst bucket gets refilled
     * @type {string}
     * @memberof PveLogRatelimitConfig
     */
    rate?: string;

}



/**
 * Check if a given object implements the PveLogRatelimitConfig interface.
 */
export function instanceOfPveLogRatelimitConfig(value: object): value is PveLogRatelimitConfig {
    
    
    if (!('enable' in value) || value['enable'] === undefined) return false;
    
    
    return true;
}

export function PveLogRatelimitConfigFromJSON(json: any): PveLogRatelimitConfig {
    return PveLogRatelimitConfigFromJSONTyped(json, false);
}

export function PveLogRatelimitConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): PveLogRatelimitConfig {
    if (json == null) {
        return json;
    }
    return {
        
        
        'burst': json['burst'] == null ? undefined : json['burst'],
        
        'enable': PveBooleanFromJSON(json['enable']),
        
        'rate': json['rate'] == null ? undefined : json['rate'],
        
    };
}

export function PveLogRatelimitConfigToJSON(json: any): PveLogRatelimitConfig {
    return PveLogRatelimitConfigToJSONTyped(json, false);
}

export function PveLogRatelimitConfigToJSONTyped(value?: PveLogRatelimitConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        
        'burst': value['burst'],
        
        'enable': PveBooleanToJSON(value['enable']),
        
        'rate': value['rate'],
        
    };
}

/**
 * Serialise a PveLogRatelimitConfig into Proxmox's CLI-style shorthand string
 * (`key=value,…`). The property marked `x-pve-default-key` is emitted
 * positionally without a `key=` prefix; aliases collapse multiple
 * property names to the same wire key.
 *
 * Example: `{ model: 'virtio', bridge: 'vmbr0', firewall: 1 }`
 *      → `'virtio,bridge=vmbr0,firewall=1'`
 */
export function PveLogRatelimitConfigToShorthand(value?: PveLogRatelimitConfig | null): string | undefined {
    if (value == null) return undefined;
    const parts: string[] = [];
    
    if (value['enable'] != null) {
        parts.push(String(value['enable']));
    }
    
    
    if (value['burst'] != null) {
        parts.push(`burst=${String(value['burst'])}`);
    }
    
    if (value['rate'] != null) {
        parts.push(`rate=${String(value['rate'])}`);
    }
    
    return parts.join(',');
}

/**
 * Parse a Proxmox shorthand string into a PveLogRatelimitConfig. Mirror of
 * PveLogRatelimitConfigToShorthand. The first comma-separated segment without
 * `=` is interpreted as the default-key value.
 */
export function PveLogRatelimitConfigFromShorthand(input?: string | null): PveLogRatelimitConfig | undefined {
    if (input == null) return undefined;
    const out: any = {};
    let defaultKeyConsumed = false;
    for (const part of input.split(',')) {
        if (!part) continue;
        const eq = part.indexOf('=');
        if (eq < 0) {
            
            if (!defaultKeyConsumed) {
                out['enable'] = part;
                defaultKeyConsumed = true;
                continue;
            }
            
            continue;
        }
        const k = part.slice(0, eq);
        const v = part.slice(eq + 1);
        out[k] = v;
    }
    return out as PveLogRatelimitConfig;
}

