/* tslint:disable */
/* eslint-disable */
/**
 * Proxmox Virtual Environment API
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { PveQemuFormatEnum } from './PveQemuFormatEnum';
import {
    PveQemuFormatEnumFromJSON,
    PveQemuFormatEnumFromJSONTyped,
    PveQemuFormatEnumToJSON,
    PveQemuFormatEnumToJSONTyped,
} from './PveQemuFormatEnum';
import type { PveVersionEnum } from './PveVersionEnum';
import {
    PveVersionEnumFromJSON,
    PveVersionEnumFromJSONTyped,
    PveVersionEnumToJSON,
    PveVersionEnumToJSONTyped,
} from './PveVersionEnum';

/**
 * 
 * @export
 * @interface PveQemuTpmstateConfig
 */
export interface PveQemuTpmstateConfig {

    /**
     * The drive's backing volume.
     * @type {string}
     * @memberof PveQemuTpmstateConfig
     */
    file: string;

    /**
     * Format of the image.
     * @type {PveQemuFormatEnum}
     * @memberof PveQemuTpmstateConfig
     */
    format?: PveQemuFormatEnum;

    /**
     * Disk size. This is purely informational and has no effect.
     * @type {string}
     * @memberof PveQemuTpmstateConfig
     */
    size?: string;

    /**
     * The TPM interface version. v2.0 is newer and should be preferred. Note that this cannot be changed later on.
     * @type {PveVersionEnum}
     * @memberof PveQemuTpmstateConfig
     */
    version?: PveVersionEnum;

    /**
     * 
     * @type {string}
     * @memberof PveQemuTpmstateConfig
     */
    volume?: string;

}



/**
 * Check if a given object implements the PveQemuTpmstateConfig interface.
 */
export function instanceOfPveQemuTpmstateConfig(value: object): value is PveQemuTpmstateConfig {
    
    if (!('file' in value) || value['file'] === undefined) return false;
    
    
    
    
    
    return true;
}

export function PveQemuTpmstateConfigFromJSON(json: any): PveQemuTpmstateConfig {
    return PveQemuTpmstateConfigFromJSONTyped(json, false);
}

export function PveQemuTpmstateConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): PveQemuTpmstateConfig {
    if (json == null) {
        return json;
    }
    return {
        
        
        'file': json['file'],
        
        'format': json['format'] == null ? undefined : PveQemuFormatEnumFromJSON(json['format']),
        
        'size': json['size'] == null ? undefined : json['size'],
        
        'version': json['version'] == null ? undefined : PveVersionEnumFromJSON(json['version']),
        
        'volume': json['volume'] == null ? undefined : json['volume'],
        
    };
}

export function PveQemuTpmstateConfigToJSON(json: any): PveQemuTpmstateConfig {
    return PveQemuTpmstateConfigToJSONTyped(json, false);
}

export function PveQemuTpmstateConfigToJSONTyped(value?: PveQemuTpmstateConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        
        'file': value['file'],
        
        'format': PveQemuFormatEnumToJSON(value['format']),
        
        'size': value['size'],
        
        'version': PveVersionEnumToJSON(value['version']),
        
        'volume': value['volume'],
        
    };
}

/**
 * Serialise a PveQemuTpmstateConfig into Proxmox's CLI-style shorthand string
 * (`key=value,…`). The property marked `x-pve-default-key` is emitted
 * positionally without a `key=` prefix; aliases collapse multiple
 * property names to the same wire key.
 *
 * Example: `{ model: 'virtio', bridge: 'vmbr0', firewall: 1 }`
 *      → `'virtio,bridge=vmbr0,firewall=1'`
 */
export function PveQemuTpmstateConfigToShorthand(value?: PveQemuTpmstateConfig | null): string | undefined {
    if (value == null) return undefined;
    const parts: string[] = [];
    
    if (value['file'] != null) {
        parts.push(String(value['file']));
    }
    
    
    if (value['format'] != null) {
        parts.push(`format=${String(value['format'])}`);
    }
    
    if (value['size'] != null) {
        parts.push(`size=${String(value['size'])}`);
    }
    
    if (value['version'] != null) {
        parts.push(`version=${String(value['version'])}`);
    }
    
    if (value['volume'] != null) {
        parts.push(`file=${String(value['volume'])}`);
    }
    
    return parts.join(',');
}

/**
 * Parse a Proxmox shorthand string into a PveQemuTpmstateConfig. Mirror of
 * PveQemuTpmstateConfigToShorthand. The first comma-separated segment without
 * `=` is interpreted as the default-key value.
 */
export function PveQemuTpmstateConfigFromShorthand(input?: string | null): PveQemuTpmstateConfig | undefined {
    if (input == null) return undefined;
    const out: any = {};
    let defaultKeyConsumed = false;
    for (const part of input.split(',')) {
        if (!part) continue;
        const eq = part.indexOf('=');
        if (eq < 0) {
            
            if (!defaultKeyConsumed) {
                out['file'] = part;
                defaultKeyConsumed = true;
                continue;
            }
            
            continue;
        }
        const k = part.slice(0, eq);
        const v = part.slice(eq + 1);
        out[k] = v;
    }
    return out as PveQemuTpmstateConfig;
}

