/* tslint:disable */
/* eslint-disable */
/**
 * Proxmox Virtual Environment API
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { PveBoolean } from './PveBoolean';
import {
    PveBooleanFromJSON,
    PveBooleanFromJSONTyped,
    PveBooleanToJSON,
    PveBooleanToJSONTyped,
} from './PveBoolean';
import type { PveClusterOptionsHaEnum } from './PveClusterOptionsHaEnum';
import {
    PveClusterOptionsHaEnumFromJSON,
    PveClusterOptionsHaEnumFromJSONTyped,
    PveClusterOptionsHaEnumToJSON,
    PveClusterOptionsHaEnumToJSONTyped,
} from './PveClusterOptionsHaEnum';

/**
 * 
 * @export
 * @interface PveCrsConfig
 */
export interface PveCrsConfig {

    /**
     * Configures how the HA manager should select nodes to start or recover services. With 'basic', only the number of services is used, with 'static', static CPU and memory configuration of services is considered.
     * @type {PveClusterOptionsHaEnum}
     * @memberof PveCrsConfig
     */
    ha?: PveClusterOptionsHaEnum;

    /**
     * Set to use CRS for selecting a suited node when a HA services request-state changes from stop to start.
     * @type {PveBoolean}
     * @memberof PveCrsConfig
     */
    haRebalanceOnStart?: PveBoolean;

}



/**
 * Check if a given object implements the PveCrsConfig interface.
 */
export function instanceOfPveCrsConfig(value: object): value is PveCrsConfig {
    
    
    
    return true;
}

export function PveCrsConfigFromJSON(json: any): PveCrsConfig {
    return PveCrsConfigFromJSONTyped(json, false);
}

export function PveCrsConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): PveCrsConfig {
    if (json == null) {
        return json;
    }
    return {
        
        
        'ha': json['ha'] == null ? undefined : PveClusterOptionsHaEnumFromJSON(json['ha']),
        
        'haRebalanceOnStart': json['ha-rebalance-on-start'] == null ? undefined : PveBooleanFromJSON(json['ha-rebalance-on-start']),
        
    };
}

export function PveCrsConfigToJSON(json: any): PveCrsConfig {
    return PveCrsConfigToJSONTyped(json, false);
}

export function PveCrsConfigToJSONTyped(value?: PveCrsConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        
        'ha': PveClusterOptionsHaEnumToJSON(value['ha']),
        
        'ha-rebalance-on-start': PveBooleanToJSON(value['haRebalanceOnStart']),
        
    };
}

/**
 * Serialise a PveCrsConfig into Proxmox's CLI-style shorthand string
 * (`key=value,…`). The property marked `x-pve-default-key` is emitted
 * positionally without a `key=` prefix; aliases collapse multiple
 * property names to the same wire key.
 *
 * Example: `{ model: 'virtio', bridge: 'vmbr0', firewall: 1 }`
 *      → `'virtio,bridge=vmbr0,firewall=1'`
 */
export function PveCrsConfigToShorthand(value?: PveCrsConfig | null): string | undefined {
    if (value == null) return undefined;
    const parts: string[] = [];
    
    
    if (value['ha'] != null) {
        parts.push(`ha=${String(value['ha'])}`);
    }
    
    if (value['haRebalanceOnStart'] != null) {
        parts.push(`ha-rebalance-on-start=${String(value['haRebalanceOnStart'])}`);
    }
    
    return parts.join(',');
}

/**
 * Parse a Proxmox shorthand string into a PveCrsConfig. Mirror of
 * PveCrsConfigToShorthand. The first comma-separated segment without
 * `=` is interpreted as the default-key value.
 */
export function PveCrsConfigFromShorthand(input?: string | null): PveCrsConfig | undefined {
    if (input == null) return undefined;
    const out: any = {};
    let defaultKeyConsumed = false;
    for (const part of input.split(',')) {
        if (!part) continue;
        const eq = part.indexOf('=');
        if (eq < 0) {
            
            continue;
        }
        const k = part.slice(0, eq);
        const v = part.slice(eq + 1);
        out[k] = v;
    }
    return out as PveCrsConfig;
}

