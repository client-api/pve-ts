/* tslint:disable */
/* eslint-disable */
/**
 * Proxmox Virtual Environment API
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * 
 * @export
 * @interface ClusterMappingCreateUsbRequestMapInnerOneOf
 */
export interface ClusterMappingCreateUsbRequestMapInnerOneOf {

    /**
     * Description of the node specific device.
     * @type {string}
     * @memberof ClusterMappingCreateUsbRequestMapInnerOneOf
     */
    description?: string;

    /**
     * The vendor and device ID that is expected. If a USB path is given, it is only used for detecting hardware changes
     * @type {string}
     * @memberof ClusterMappingCreateUsbRequestMapInnerOneOf
     */
    id: string;

    /**
     * The cluster node name.
     * @type {string}
     * @memberof ClusterMappingCreateUsbRequestMapInnerOneOf
     */
    node: string;

    /**
     * The path to the usb device.
     * @type {string}
     * @memberof ClusterMappingCreateUsbRequestMapInnerOneOf
     */
    path?: string;

}

/**
 * Check if a given object implements the ClusterMappingCreateUsbRequestMapInnerOneOf interface.
 */
export function instanceOfClusterMappingCreateUsbRequestMapInnerOneOf(value: object): value is ClusterMappingCreateUsbRequestMapInnerOneOf {
    
    
    if (!('id' in value) || value['id'] === undefined) return false;
    
    if (!('node' in value) || value['node'] === undefined) return false;
    
    
    return true;
}

export function ClusterMappingCreateUsbRequestMapInnerOneOfFromJSON(json: any): ClusterMappingCreateUsbRequestMapInnerOneOf {
    return ClusterMappingCreateUsbRequestMapInnerOneOfFromJSONTyped(json, false);
}

export function ClusterMappingCreateUsbRequestMapInnerOneOfFromJSONTyped(json: any, ignoreDiscriminator: boolean): ClusterMappingCreateUsbRequestMapInnerOneOf {
    if (json == null) {
        return json;
    }
    return {
        
        
        'description': json['description'] == null ? undefined : json['description'],
        
        'id': json['id'],
        
        'node': json['node'],
        
        'path': json['path'] == null ? undefined : json['path'],
        
    };
}

export function ClusterMappingCreateUsbRequestMapInnerOneOfToJSON(json: any): ClusterMappingCreateUsbRequestMapInnerOneOf {
    return ClusterMappingCreateUsbRequestMapInnerOneOfToJSONTyped(json, false);
}

export function ClusterMappingCreateUsbRequestMapInnerOneOfToJSONTyped(value?: ClusterMappingCreateUsbRequestMapInnerOneOf | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        
        'description': value['description'],
        
        'id': value['id'],
        
        'node': value['node'],
        
        'path': value['path'],
        
    };
}

/**
 * Serialise a ClusterMappingCreateUsbRequestMapInnerOneOf into Proxmox's CLI-style shorthand string
 * (`key=value,…`). The property marked `x-pve-default-key` is emitted
 * positionally without a `key=` prefix; aliases collapse multiple
 * property names to the same wire key.
 *
 * Example: `{ model: 'virtio', bridge: 'vmbr0', firewall: 1 }`
 *      → `'virtio,bridge=vmbr0,firewall=1'`
 */
export function ClusterMappingCreateUsbRequestMapInnerOneOfToShorthand(value?: ClusterMappingCreateUsbRequestMapInnerOneOf | null): string | undefined {
    if (value == null) return undefined;
    const parts: string[] = [];
    
    
    if (value['description'] != null) {
        parts.push(`description=${String(value['description'])}`);
    }
    
    if (value['id'] != null) {
        parts.push(`id=${String(value['id'])}`);
    }
    
    if (value['node'] != null) {
        parts.push(`node=${String(value['node'])}`);
    }
    
    if (value['path'] != null) {
        parts.push(`path=${String(value['path'])}`);
    }
    
    return parts.join(',');
}

/**
 * Parse a Proxmox shorthand string into a ClusterMappingCreateUsbRequestMapInnerOneOf. Mirror of
 * ClusterMappingCreateUsbRequestMapInnerOneOfToShorthand. The first comma-separated segment without
 * `=` is interpreted as the default-key value.
 */
export function ClusterMappingCreateUsbRequestMapInnerOneOfFromShorthand(input?: string | null): ClusterMappingCreateUsbRequestMapInnerOneOf | undefined {
    if (input == null) return undefined;
    const out: any = {};
    let defaultKeyConsumed = false;
    for (const part of input.split(',')) {
        if (!part) continue;
        const eq = part.indexOf('=');
        if (eq < 0) {
            
            continue;
        }
        const k = part.slice(0, eq);
        const v = part.slice(eq + 1);
        out[k] = v;
    }
    return out as ClusterMappingCreateUsbRequestMapInnerOneOf;
}

