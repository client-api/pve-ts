/* tslint:disable */
/* eslint-disable */
/**
 * Proxmox Virtual Environment API
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { PveBoolean } from './PveBoolean';
import {
    PveBooleanFromJSON,
    PveBooleanFromJSONTyped,
    PveBooleanToJSON,
    PveBooleanToJSONTyped,
} from './PveBoolean';

/**
 * 
 * @export
 * @interface PveMpConfig
 */
export interface PveMpConfig {

    /**
     * Explicitly enable or disable ACL support.
     * @type {PveBoolean}
     * @memberof PveMpConfig
     */
    acl?: PveBoolean;

    /**
     * Whether to include the mount point in backups (only used for volume mount points).
     * @type {PveBoolean}
     * @memberof PveMpConfig
     */
    backup?: PveBoolean;

    /**
     * Extra mount options for rootfs/mps.
     * @type {string}
     * @memberof PveMpConfig
     */
    mountoptions?: string;

    /**
     * Path to the mount point as seen from inside the container.
     * 
     * NOTE: Must not contain any symlinks for security reasons.
     * @type {string}
     * @memberof PveMpConfig
     */
    mp: string;

    /**
     * Enable user quotas inside the container (not supported with zfs subvolumes)
     * @type {PveBoolean}
     * @memberof PveMpConfig
     */
    quota?: PveBoolean;

    /**
     * Will include this volume to a storage replica job.
     * @type {PveBoolean}
     * @memberof PveMpConfig
     */
    replicate?: PveBoolean;

    /**
     * Read-only mount point
     * @type {PveBoolean}
     * @memberof PveMpConfig
     */
    ro?: PveBoolean;

    /**
     * Mark this non-volume mount point as available on all nodes.
     * 
     * WARNING: This option does not share the mount point automatically, it assumes it is shared already!
     * @type {PveBoolean}
     * @memberof PveMpConfig
     */
    shared?: PveBoolean;

    /**
     * Volume size (read only value).
     * @type {string}
     * @memberof PveMpConfig
     */
    size?: string;

    /**
     * Volume, device or directory to mount into the container.
     * @type {string}
     * @memberof PveMpConfig
     */
    volume: string;

}



/**
 * Check if a given object implements the PveMpConfig interface.
 */
export function instanceOfPveMpConfig(value: object): value is PveMpConfig {
    
    
    
    
    if (!('mp' in value) || value['mp'] === undefined) return false;
    
    
    
    
    
    
    if (!('volume' in value) || value['volume'] === undefined) return false;
    
    return true;
}

export function PveMpConfigFromJSON(json: any): PveMpConfig {
    return PveMpConfigFromJSONTyped(json, false);
}

export function PveMpConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): PveMpConfig {
    if (json == null) {
        return json;
    }
    return {
        
        
        'acl': json['acl'] == null ? undefined : PveBooleanFromJSON(json['acl']),
        
        'backup': json['backup'] == null ? undefined : PveBooleanFromJSON(json['backup']),
        
        'mountoptions': json['mountoptions'] == null ? undefined : json['mountoptions'],
        
        'mp': json['mp'],
        
        'quota': json['quota'] == null ? undefined : PveBooleanFromJSON(json['quota']),
        
        'replicate': json['replicate'] == null ? undefined : PveBooleanFromJSON(json['replicate']),
        
        'ro': json['ro'] == null ? undefined : PveBooleanFromJSON(json['ro']),
        
        'shared': json['shared'] == null ? undefined : PveBooleanFromJSON(json['shared']),
        
        'size': json['size'] == null ? undefined : json['size'],
        
        'volume': json['volume'],
        
    };
}

export function PveMpConfigToJSON(json: any): PveMpConfig {
    return PveMpConfigToJSONTyped(json, false);
}

export function PveMpConfigToJSONTyped(value?: PveMpConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        
        'acl': PveBooleanToJSON(value['acl']),
        
        'backup': PveBooleanToJSON(value['backup']),
        
        'mountoptions': value['mountoptions'],
        
        'mp': value['mp'],
        
        'quota': PveBooleanToJSON(value['quota']),
        
        'replicate': PveBooleanToJSON(value['replicate']),
        
        'ro': PveBooleanToJSON(value['ro']),
        
        'shared': PveBooleanToJSON(value['shared']),
        
        'size': value['size'],
        
        'volume': value['volume'],
        
    };
}

/**
 * Serialise a PveMpConfig into Proxmox's CLI-style shorthand string
 * (`key=value,…`). The property marked `x-pve-default-key` is emitted
 * positionally without a `key=` prefix; aliases collapse multiple
 * property names to the same wire key.
 *
 * Example: `{ model: 'virtio', bridge: 'vmbr0', firewall: 1 }`
 *      → `'virtio,bridge=vmbr0,firewall=1'`
 */
export function PveMpConfigToShorthand(value?: PveMpConfig | null): string | undefined {
    if (value == null) return undefined;
    const parts: string[] = [];
    
    if (value['volume'] != null) {
        parts.push(String(value['volume']));
    }
    
    
    if (value['acl'] != null) {
        parts.push(`acl=${String(value['acl'])}`);
    }
    
    if (value['backup'] != null) {
        parts.push(`backup=${String(value['backup'])}`);
    }
    
    if (value['mountoptions'] != null) {
        parts.push(`mountoptions=${String(value['mountoptions'])}`);
    }
    
    if (value['mp'] != null) {
        parts.push(`mp=${String(value['mp'])}`);
    }
    
    if (value['quota'] != null) {
        parts.push(`quota=${String(value['quota'])}`);
    }
    
    if (value['replicate'] != null) {
        parts.push(`replicate=${String(value['replicate'])}`);
    }
    
    if (value['ro'] != null) {
        parts.push(`ro=${String(value['ro'])}`);
    }
    
    if (value['shared'] != null) {
        parts.push(`shared=${String(value['shared'])}`);
    }
    
    if (value['size'] != null) {
        parts.push(`size=${String(value['size'])}`);
    }
    
    return parts.join(',');
}

/**
 * Parse a Proxmox shorthand string into a PveMpConfig. Mirror of
 * PveMpConfigToShorthand. The first comma-separated segment without
 * `=` is interpreted as the default-key value.
 */
export function PveMpConfigFromShorthand(input?: string | null): PveMpConfig | undefined {
    if (input == null) return undefined;
    const out: any = {};
    let defaultKeyConsumed = false;
    for (const part of input.split(',')) {
        if (!part) continue;
        const eq = part.indexOf('=');
        if (eq < 0) {
            
            if (!defaultKeyConsumed) {
                out['volume'] = part;
                defaultKeyConsumed = true;
                continue;
            }
            
            continue;
        }
        const k = part.slice(0, eq);
        const v = part.slice(eq + 1);
        out[k] = v;
    }
    return out as PveMpConfig;
}

