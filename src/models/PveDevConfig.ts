/* tslint:disable */
/* eslint-disable */
/**
 * Proxmox Virtual Environment API
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { PveBoolean } from './PveBoolean';
import {
    PveBooleanFromJSON,
    PveBooleanFromJSONTyped,
    PveBooleanToJSON,
    PveBooleanToJSONTyped,
} from './PveBoolean';

/**
 * 
 * @export
 * @interface PveDevConfig
 */
export interface PveDevConfig {

    /**
     * Deny the container to write to the device
     * @type {PveBoolean}
     * @memberof PveDevConfig
     */
    denyWrite?: PveBoolean;

    /**
     * Group ID to be assigned to the device node
     * @type {number}
     * @memberof PveDevConfig
     */
    gid?: number;

    /**
     * Access mode to be set on the device node
     * @type {string}
     * @memberof PveDevConfig
     */
    mode?: string;

    /**
     * Path to the device to pass through to the container
     * @type {string}
     * @memberof PveDevConfig
     */
    path?: string;

    /**
     * User ID to be assigned to the device node
     * @type {number}
     * @memberof PveDevConfig
     */
    uid?: number;

}



/**
 * Check if a given object implements the PveDevConfig interface.
 */
export function instanceOfPveDevConfig(value: object): value is PveDevConfig {
    
    
    
    
    
    
    return true;
}

export function PveDevConfigFromJSON(json: any): PveDevConfig {
    return PveDevConfigFromJSONTyped(json, false);
}

export function PveDevConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): PveDevConfig {
    if (json == null) {
        return json;
    }
    return {
        
        
        'denyWrite': json['deny-write'] == null ? undefined : PveBooleanFromJSON(json['deny-write']),
        
        'gid': json['gid'] == null ? undefined : json['gid'],
        
        'mode': json['mode'] == null ? undefined : json['mode'],
        
        'path': json['path'] == null ? undefined : json['path'],
        
        'uid': json['uid'] == null ? undefined : json['uid'],
        
    };
}

export function PveDevConfigToJSON(json: any): PveDevConfig {
    return PveDevConfigToJSONTyped(json, false);
}

export function PveDevConfigToJSONTyped(value?: PveDevConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        
        'deny-write': PveBooleanToJSON(value['denyWrite']),
        
        'gid': value['gid'],
        
        'mode': value['mode'],
        
        'path': value['path'],
        
        'uid': value['uid'],
        
    };
}

/**
 * Serialise a PveDevConfig into Proxmox's CLI-style shorthand string
 * (`key=value,…`). The property marked `x-pve-default-key` is emitted
 * positionally without a `key=` prefix; aliases collapse multiple
 * property names to the same wire key.
 *
 * Example: `{ model: 'virtio', bridge: 'vmbr0', firewall: 1 }`
 *      → `'virtio,bridge=vmbr0,firewall=1'`
 */
export function PveDevConfigToShorthand(value?: PveDevConfig | null): string | undefined {
    if (value == null) return undefined;
    const parts: string[] = [];
    
    if (value['path'] != null) {
        parts.push(String(value['path']));
    }
    
    
    if (value['denyWrite'] != null) {
        parts.push(`deny-write=${String(value['denyWrite'])}`);
    }
    
    if (value['gid'] != null) {
        parts.push(`gid=${String(value['gid'])}`);
    }
    
    if (value['mode'] != null) {
        parts.push(`mode=${String(value['mode'])}`);
    }
    
    if (value['uid'] != null) {
        parts.push(`uid=${String(value['uid'])}`);
    }
    
    return parts.join(',');
}

/**
 * Parse a Proxmox shorthand string into a PveDevConfig. Mirror of
 * PveDevConfigToShorthand. The first comma-separated segment without
 * `=` is interpreted as the default-key value.
 */
export function PveDevConfigFromShorthand(input?: string | null): PveDevConfig | undefined {
    if (input == null) return undefined;
    const out: any = {};
    let defaultKeyConsumed = false;
    for (const part of input.split(',')) {
        if (!part) continue;
        const eq = part.indexOf('=');
        if (eq < 0) {
            
            if (!defaultKeyConsumed) {
                out['path'] = part;
                defaultKeyConsumed = true;
                continue;
            }
            
            continue;
        }
        const k = part.slice(0, eq);
        const v = part.slice(eq + 1);
        out[k] = v;
    }
    return out as PveDevConfig;
}

