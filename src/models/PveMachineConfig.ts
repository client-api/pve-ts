/* tslint:disable */
/* eslint-disable */
/**
 * Proxmox Virtual Environment API
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { PveBoolean } from './PveBoolean';
import {
    PveBooleanFromJSON,
    PveBooleanFromJSONTyped,
    PveBooleanToJSON,
    PveBooleanToJSONTyped,
} from './PveBoolean';
import type { PveViommuEnum } from './PveViommuEnum';
import {
    PveViommuEnumFromJSON,
    PveViommuEnumFromJSONTyped,
    PveViommuEnumToJSON,
    PveViommuEnumToJSONTyped,
} from './PveViommuEnum';

/**
 * 
 * @export
 * @interface PveMachineConfig
 */
export interface PveMachineConfig {

    /**
     * Specifies the vIOMMU address space bit width.
     * 
     * Intel vIOMMU supports a bit width of either 39 or 48 bits and VirtIO vIOMMU supports any bit width between 32 and 64 bits.
     * @type {number}
     * @memberof PveMachineConfig
     */
    awBits?: number;

    /**
     * Enables S3 power state. Defaults to false beginning with machine types 9.2+pve1, true before.
     * @type {PveBoolean}
     * @memberof PveMachineConfig
     */
    enableS3?: PveBoolean;

    /**
     * Enables S4 power state. Defaults to false beginning with machine types 9.2+pve1, true before.
     * @type {PveBoolean}
     * @memberof PveMachineConfig
     */
    enableS4?: PveBoolean;

    /**
     * Specifies the QEMU machine type.
     * @type {string}
     * @memberof PveMachineConfig
     */
    type?: string;

    /**
     * Enable and set guest vIOMMU variant (Intel vIOMMU needs q35 to be set as machine type).
     * @type {PveViommuEnum}
     * @memberof PveMachineConfig
     */
    viommu?: PveViommuEnum;

}



/**
 * Check if a given object implements the PveMachineConfig interface.
 */
export function instanceOfPveMachineConfig(value: object): value is PveMachineConfig {
    
    
    
    
    
    
    return true;
}

export function PveMachineConfigFromJSON(json: any): PveMachineConfig {
    return PveMachineConfigFromJSONTyped(json, false);
}

export function PveMachineConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): PveMachineConfig {
    if (json == null) {
        return json;
    }
    return {
        
        
        'awBits': json['aw-bits'] == null ? undefined : json['aw-bits'],
        
        'enableS3': json['enable-s3'] == null ? undefined : PveBooleanFromJSON(json['enable-s3']),
        
        'enableS4': json['enable-s4'] == null ? undefined : PveBooleanFromJSON(json['enable-s4']),
        
        'type': json['type'] == null ? undefined : json['type'],
        
        'viommu': json['viommu'] == null ? undefined : PveViommuEnumFromJSON(json['viommu']),
        
    };
}

export function PveMachineConfigToJSON(json: any): PveMachineConfig {
    return PveMachineConfigToJSONTyped(json, false);
}

export function PveMachineConfigToJSONTyped(value?: PveMachineConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        
        'aw-bits': value['awBits'],
        
        'enable-s3': PveBooleanToJSON(value['enableS3']),
        
        'enable-s4': PveBooleanToJSON(value['enableS4']),
        
        'type': value['type'],
        
        'viommu': PveViommuEnumToJSON(value['viommu']),
        
    };
}

/**
 * Serialise a PveMachineConfig into Proxmox's CLI-style shorthand string
 * (`key=value,…`). The property marked `x-pve-default-key` is emitted
 * positionally without a `key=` prefix; aliases collapse multiple
 * property names to the same wire key.
 *
 * Example: `{ model: 'virtio', bridge: 'vmbr0', firewall: 1 }`
 *      → `'virtio,bridge=vmbr0,firewall=1'`
 */
export function PveMachineConfigToShorthand(value?: PveMachineConfig | null): string | undefined {
    if (value == null) return undefined;
    const parts: string[] = [];
    
    if (value['type'] != null) {
        parts.push(String(value['type']));
    }
    
    
    if (value['awBits'] != null) {
        parts.push(`aw-bits=${String(value['awBits'])}`);
    }
    
    if (value['enableS3'] != null) {
        parts.push(`enable-s3=${String(value['enableS3'])}`);
    }
    
    if (value['enableS4'] != null) {
        parts.push(`enable-s4=${String(value['enableS4'])}`);
    }
    
    if (value['viommu'] != null) {
        parts.push(`viommu=${String(value['viommu'])}`);
    }
    
    return parts.join(',');
}

/**
 * Parse a Proxmox shorthand string into a PveMachineConfig. Mirror of
 * PveMachineConfigToShorthand. The first comma-separated segment without
 * `=` is interpreted as the default-key value.
 */
export function PveMachineConfigFromShorthand(input?: string | null): PveMachineConfig | undefined {
    if (input == null) return undefined;
    const out: any = {};
    let defaultKeyConsumed = false;
    for (const part of input.split(',')) {
        if (!part) continue;
        const eq = part.indexOf('=');
        if (eq < 0) {
            
            if (!defaultKeyConsumed) {
                out['type'] = part;
                defaultKeyConsumed = true;
                continue;
            }
            
            continue;
        }
        const k = part.slice(0, eq);
        const v = part.slice(eq + 1);
        out[k] = v;
    }
    return out as PveMachineConfig;
}

