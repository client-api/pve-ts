/* tslint:disable */
/* eslint-disable */
/**
 * Proxmox Virtual Environment API
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { PveBoolean } from './PveBoolean';
import {
    PveBooleanFromJSON,
    PveBooleanFromJSONTyped,
    PveBooleanToJSON,
    PveBooleanToJSONTyped,
} from './PveBoolean';
import type { PveQemuTypeEnum } from './PveQemuTypeEnum';
import {
    PveQemuTypeEnumFromJSON,
    PveQemuTypeEnumFromJSONTyped,
    PveQemuTypeEnumToJSON,
    PveQemuTypeEnumToJSONTyped,
} from './PveQemuTypeEnum';

/**
 * 
 * @export
 * @interface PveAgentConfig
 */
export interface PveAgentConfig {

    /**
     * Enable/disable communication with a QEMU Guest Agent (QGA) running in the VM.
     * @type {PveBoolean}
     * @memberof PveAgentConfig
     */
    enabled: PveBoolean;

    /**
     * Freeze/thaw guest filesystems on backup for consistency.
     * @type {PveBoolean}
     * @memberof PveAgentConfig
     */
    freezeFsOnBackup?: PveBoolean;

    /**
     * Run fstrim after moving a disk or migrating the VM.
     * @type {PveBoolean}
     * @memberof PveAgentConfig
     */
    fstrimClonedDisks?: PveBoolean;

    /**
     * Select the agent type
     * @type {PveQemuTypeEnum}
     * @memberof PveAgentConfig
     */
    type?: PveQemuTypeEnum;

}



/**
 * Check if a given object implements the PveAgentConfig interface.
 */
export function instanceOfPveAgentConfig(value: object): value is PveAgentConfig {
    
    if (!('enabled' in value) || value['enabled'] === undefined) return false;
    
    
    
    
    return true;
}

export function PveAgentConfigFromJSON(json: any): PveAgentConfig {
    return PveAgentConfigFromJSONTyped(json, false);
}

export function PveAgentConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): PveAgentConfig {
    if (json == null) {
        return json;
    }
    return {
        
        
        'enabled': PveBooleanFromJSON(json['enabled']),
        
        'freezeFsOnBackup': json['freeze-fs-on-backup'] == null ? undefined : PveBooleanFromJSON(json['freeze-fs-on-backup']),
        
        'fstrimClonedDisks': json['fstrim_cloned_disks'] == null ? undefined : PveBooleanFromJSON(json['fstrim_cloned_disks']),
        
        'type': json['type'] == null ? undefined : PveQemuTypeEnumFromJSON(json['type']),
        
    };
}

export function PveAgentConfigToJSON(json: any): PveAgentConfig {
    return PveAgentConfigToJSONTyped(json, false);
}

export function PveAgentConfigToJSONTyped(value?: PveAgentConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        
        'enabled': PveBooleanToJSON(value['enabled']),
        
        'freeze-fs-on-backup': PveBooleanToJSON(value['freezeFsOnBackup']),
        
        'fstrim_cloned_disks': PveBooleanToJSON(value['fstrimClonedDisks']),
        
        'type': PveQemuTypeEnumToJSON(value['type']),
        
    };
}

/**
 * Serialise a PveAgentConfig into Proxmox's CLI-style shorthand string
 * (`key=value,…`). The property marked `x-pve-default-key` is emitted
 * positionally without a `key=` prefix; aliases collapse multiple
 * property names to the same wire key.
 *
 * Example: `{ model: 'virtio', bridge: 'vmbr0', firewall: 1 }`
 *      → `'virtio,bridge=vmbr0,firewall=1'`
 */
export function PveAgentConfigToShorthand(value?: PveAgentConfig | null): string | undefined {
    if (value == null) return undefined;
    const parts: string[] = [];
    
    if (value['enabled'] != null) {
        parts.push(String(value['enabled']));
    }
    
    
    if (value['freezeFsOnBackup'] != null) {
        parts.push(`freeze-fs-on-backup=${String(value['freezeFsOnBackup'])}`);
    }
    
    if (value['fstrimClonedDisks'] != null) {
        parts.push(`fstrim_cloned_disks=${String(value['fstrimClonedDisks'])}`);
    }
    
    if (value['type'] != null) {
        parts.push(`type=${String(value['type'])}`);
    }
    
    return parts.join(',');
}

/**
 * Parse a Proxmox shorthand string into a PveAgentConfig. Mirror of
 * PveAgentConfigToShorthand. The first comma-separated segment without
 * `=` is interpreted as the default-key value.
 */
export function PveAgentConfigFromShorthand(input?: string | null): PveAgentConfig | undefined {
    if (input == null) return undefined;
    const out: any = {};
    let defaultKeyConsumed = false;
    for (const part of input.split(',')) {
        if (!part) continue;
        const eq = part.indexOf('=');
        if (eq < 0) {
            
            if (!defaultKeyConsumed) {
                out['enabled'] = part;
                defaultKeyConsumed = true;
                continue;
            }
            
            continue;
        }
        const k = part.slice(0, eq);
        const v = part.slice(eq + 1);
        out[k] = v;
    }
    return out as PveAgentConfig;
}

