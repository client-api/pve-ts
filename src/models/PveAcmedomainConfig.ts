/* tslint:disable */
/* eslint-disable */
/**
 * Proxmox Virtual Environment API
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * 
 * @export
 * @interface PveAcmedomainConfig
 */
export interface PveAcmedomainConfig {

    /**
     * Alias for the Domain to verify ACME Challenge over DNS
     * @type {string}
     * @memberof PveAcmedomainConfig
     */
    alias?: string;

    /**
     * domain for this node's ACME certificate
     * @type {string}
     * @memberof PveAcmedomainConfig
     */
    domain: string;

    /**
     * The ACME plugin ID
     * @type {string}
     * @memberof PveAcmedomainConfig
     */
    plugin?: string;

}

/**
 * Check if a given object implements the PveAcmedomainConfig interface.
 */
export function instanceOfPveAcmedomainConfig(value: object): value is PveAcmedomainConfig {
    
    
    if (!('domain' in value) || value['domain'] === undefined) return false;
    
    
    return true;
}

export function PveAcmedomainConfigFromJSON(json: any): PveAcmedomainConfig {
    return PveAcmedomainConfigFromJSONTyped(json, false);
}

export function PveAcmedomainConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): PveAcmedomainConfig {
    if (json == null) {
        return json;
    }
    return {
        
        
        'alias': json['alias'] == null ? undefined : json['alias'],
        
        'domain': json['domain'],
        
        'plugin': json['plugin'] == null ? undefined : json['plugin'],
        
    };
}

export function PveAcmedomainConfigToJSON(json: any): PveAcmedomainConfig {
    return PveAcmedomainConfigToJSONTyped(json, false);
}

export function PveAcmedomainConfigToJSONTyped(value?: PveAcmedomainConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        
        'alias': value['alias'],
        
        'domain': value['domain'],
        
        'plugin': value['plugin'],
        
    };
}

/**
 * Serialise a PveAcmedomainConfig into Proxmox's CLI-style shorthand string
 * (`key=value,…`). The property marked `x-pve-default-key` is emitted
 * positionally without a `key=` prefix; aliases collapse multiple
 * property names to the same wire key.
 *
 * Example: `{ model: 'virtio', bridge: 'vmbr0', firewall: 1 }`
 *      → `'virtio,bridge=vmbr0,firewall=1'`
 */
export function PveAcmedomainConfigToShorthand(value?: PveAcmedomainConfig | null): string | undefined {
    if (value == null) return undefined;
    const parts: string[] = [];
    
    if (value['domain'] != null) {
        parts.push(String(value['domain']));
    }
    
    
    if (value['alias'] != null) {
        parts.push(`alias=${String(value['alias'])}`);
    }
    
    if (value['plugin'] != null) {
        parts.push(`plugin=${String(value['plugin'])}`);
    }
    
    return parts.join(',');
}

/**
 * Parse a Proxmox shorthand string into a PveAcmedomainConfig. Mirror of
 * PveAcmedomainConfigToShorthand. The first comma-separated segment without
 * `=` is interpreted as the default-key value.
 */
export function PveAcmedomainConfigFromShorthand(input?: string | null): PveAcmedomainConfig | undefined {
    if (input == null) return undefined;
    const out: any = {};
    let defaultKeyConsumed = false;
    for (const part of input.split(',')) {
        if (!part) continue;
        const eq = part.indexOf('=');
        if (eq < 0) {
            
            if (!defaultKeyConsumed) {
                out['domain'] = part;
                defaultKeyConsumed = true;
                continue;
            }
            
            continue;
        }
        const k = part.slice(0, eq);
        const v = part.slice(eq + 1);
        out[k] = v;
    }
    return out as PveAcmedomainConfig;
}

