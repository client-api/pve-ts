/* tslint:disable */
/* eslint-disable */
/**
 * Proxmox Virtual Environment API
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
/**
 * 
 * @export
 * @interface PveWakeonlanConfig
 */
export interface PveWakeonlanConfig {

    /**
     * Bind to this interface when sending wake on LAN packet
     * @type {string}
     * @memberof PveWakeonlanConfig
     */
    bindInterface?: string;

    /**
     * IPv4 broadcast address to use when sending wake on LAN packet
     * @type {string}
     * @memberof PveWakeonlanConfig
     */
    broadcastAddress?: string;

    /**
     * MAC address for wake on LAN
     * @type {string}
     * @memberof PveWakeonlanConfig
     */
    mac: string;

}

/**
 * Check if a given object implements the PveWakeonlanConfig interface.
 */
export function instanceOfPveWakeonlanConfig(value: object): value is PveWakeonlanConfig {
    
    
    
    if (!('mac' in value) || value['mac'] === undefined) return false;
    
    return true;
}

export function PveWakeonlanConfigFromJSON(json: any): PveWakeonlanConfig {
    return PveWakeonlanConfigFromJSONTyped(json, false);
}

export function PveWakeonlanConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): PveWakeonlanConfig {
    if (json == null) {
        return json;
    }
    return {
        
        
        'bindInterface': json['bind-interface'] == null ? undefined : json['bind-interface'],
        
        'broadcastAddress': json['broadcast-address'] == null ? undefined : json['broadcast-address'],
        
        'mac': json['mac'],
        
    };
}

export function PveWakeonlanConfigToJSON(json: any): PveWakeonlanConfig {
    return PveWakeonlanConfigToJSONTyped(json, false);
}

export function PveWakeonlanConfigToJSONTyped(value?: PveWakeonlanConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        
        'bind-interface': value['bindInterface'],
        
        'broadcast-address': value['broadcastAddress'],
        
        'mac': value['mac'],
        
    };
}

/**
 * Serialise a PveWakeonlanConfig into Proxmox's CLI-style shorthand string
 * (`key=value,…`). The property marked `x-pve-default-key` is emitted
 * positionally without a `key=` prefix; aliases collapse multiple
 * property names to the same wire key.
 *
 * Example: `{ model: 'virtio', bridge: 'vmbr0', firewall: 1 }`
 *      → `'virtio,bridge=vmbr0,firewall=1'`
 */
export function PveWakeonlanConfigToShorthand(value?: PveWakeonlanConfig | null): string | undefined {
    if (value == null) return undefined;
    const parts: string[] = [];
    
    if (value['mac'] != null) {
        parts.push(String(value['mac']));
    }
    
    
    if (value['bindInterface'] != null) {
        parts.push(`bind-interface=${String(value['bindInterface'])}`);
    }
    
    if (value['broadcastAddress'] != null) {
        parts.push(`broadcast-address=${String(value['broadcastAddress'])}`);
    }
    
    return parts.join(',');
}

/**
 * Parse a Proxmox shorthand string into a PveWakeonlanConfig. Mirror of
 * PveWakeonlanConfigToShorthand. The first comma-separated segment without
 * `=` is interpreted as the default-key value.
 */
export function PveWakeonlanConfigFromShorthand(input?: string | null): PveWakeonlanConfig | undefined {
    if (input == null) return undefined;
    const out: any = {};
    let defaultKeyConsumed = false;
    for (const part of input.split(',')) {
        if (!part) continue;
        const eq = part.indexOf('=');
        if (eq < 0) {
            
            if (!defaultKeyConsumed) {
                out['mac'] = part;
                defaultKeyConsumed = true;
                continue;
            }
            
            continue;
        }
        const k = part.slice(0, eq);
        const v = part.slice(eq + 1);
        out[k] = v;
    }
    return out as PveWakeonlanConfig;
}

