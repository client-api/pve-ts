/* tslint:disable */
/* eslint-disable */
/**
 * Proxmox Virtual Environment API
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { PveMigrationTypeEnum } from './PveMigrationTypeEnum';
import {
    PveMigrationTypeEnumFromJSON,
    PveMigrationTypeEnumFromJSONTyped,
    PveMigrationTypeEnumToJSON,
    PveMigrationTypeEnumToJSONTyped,
} from './PveMigrationTypeEnum';

/**
 * 
 * @export
 * @interface PveMigrationConfig
 */
export interface PveMigrationConfig {

    /**
     * CIDR of the (sub) network that is used for migration. Used as a fallback for replications jobs if the replication network setting is not set
     * @type {string}
     * @memberof PveMigrationConfig
     */
    network?: string;

    /**
     * Migration traffic is encrypted using an SSH tunnel by default. On secure, completely private networks this can be disabled to increase performance.
     * @type {PveMigrationTypeEnum}
     * @memberof PveMigrationConfig
     */
    type: PveMigrationTypeEnum;

}



/**
 * Check if a given object implements the PveMigrationConfig interface.
 */
export function instanceOfPveMigrationConfig(value: object): value is PveMigrationConfig {
    
    
    if (!('type' in value) || value['type'] === undefined) return false;
    
    return true;
}

export function PveMigrationConfigFromJSON(json: any): PveMigrationConfig {
    return PveMigrationConfigFromJSONTyped(json, false);
}

export function PveMigrationConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): PveMigrationConfig {
    if (json == null) {
        return json;
    }
    return {
        
        
        'network': json['network'] == null ? undefined : json['network'],
        
        'type': PveMigrationTypeEnumFromJSON(json['type']),
        
    };
}

export function PveMigrationConfigToJSON(json: any): PveMigrationConfig {
    return PveMigrationConfigToJSONTyped(json, false);
}

export function PveMigrationConfigToJSONTyped(value?: PveMigrationConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        
        'network': value['network'],
        
        'type': PveMigrationTypeEnumToJSON(value['type']),
        
    };
}

/**
 * Serialise a PveMigrationConfig into Proxmox's CLI-style shorthand string
 * (`key=value,…`). The property marked `x-pve-default-key` is emitted
 * positionally without a `key=` prefix; aliases collapse multiple
 * property names to the same wire key.
 *
 * Example: `{ model: 'virtio', bridge: 'vmbr0', firewall: 1 }`
 *      → `'virtio,bridge=vmbr0,firewall=1'`
 */
export function PveMigrationConfigToShorthand(value?: PveMigrationConfig | null): string | undefined {
    if (value == null) return undefined;
    const parts: string[] = [];
    
    if (value['type'] != null) {
        parts.push(String(value['type']));
    }
    
    
    if (value['network'] != null) {
        parts.push(`network=${String(value['network'])}`);
    }
    
    return parts.join(',');
}

/**
 * Parse a Proxmox shorthand string into a PveMigrationConfig. Mirror of
 * PveMigrationConfigToShorthand. The first comma-separated segment without
 * `=` is interpreted as the default-key value.
 */
export function PveMigrationConfigFromShorthand(input?: string | null): PveMigrationConfig | undefined {
    if (input == null) return undefined;
    const out: any = {};
    let defaultKeyConsumed = false;
    for (const part of input.split(',')) {
        if (!part) continue;
        const eq = part.indexOf('=');
        if (eq < 0) {
            
            if (!defaultKeyConsumed) {
                out['type'] = part;
                defaultKeyConsumed = true;
                continue;
            }
            
            continue;
        }
        const k = part.slice(0, eq);
        const v = part.slice(eq + 1);
        out[k] = v;
    }
    return out as PveMigrationConfig;
}

