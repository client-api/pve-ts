/* tslint:disable */
/* eslint-disable */
/**
 * Proxmox Virtual Environment API
 * Generated from apidoc.js. NOT an official Proxmox specification. See https://pve.proxmox.com/pve-docs/api-viewer/ for the upstream documentation.
 *
 * The version of the OpenAPI document: 8.x
 * 
 *
 * NOTE: This class is auto generated by OpenAPI Generator (https://openapi-generator.tech).
 * https://openapi-generator.tech
 * Do not edit the class manually.
 */

import { mapValues } from '../runtime';
import type { PveClusterOptionsShutdownPolicyEnum } from './PveClusterOptionsShutdownPolicyEnum';
import {
    PveClusterOptionsShutdownPolicyEnumFromJSON,
    PveClusterOptionsShutdownPolicyEnumFromJSONTyped,
    PveClusterOptionsShutdownPolicyEnumToJSON,
    PveClusterOptionsShutdownPolicyEnumToJSONTyped,
} from './PveClusterOptionsShutdownPolicyEnum';

/**
 * 
 * @export
 * @interface PveHaConfig
 */
export interface PveHaConfig {

    /**
     * Describes the policy for handling HA services on poweroff or reboot of a node. Freeze will always freeze services which are still located on the node on shutdown, those services won't be recovered by the HA manager. Failover will not mark the services as frozen and thus the services will get recovered to other nodes, if the shutdown node does not come up again quickly (< 1min). 'conditional' chooses automatically depending on the type of shutdown, i.e., on a reboot the service will be frozen but on a poweroff the service will stay as is, and thus get recovered after about 2 minutes. Migrate will try to move all running services to another node when a reboot or shutdown was triggered. The poweroff process will only continue once no running services are located on the node anymore. If the node comes up again, the service will be moved back to the previously powered-off node, at least if no other migration, reloaction or recovery took place.
     * @type {PveClusterOptionsShutdownPolicyEnum}
     * @memberof PveHaConfig
     */
    shutdownPolicy: PveClusterOptionsShutdownPolicyEnum;

}



/**
 * Check if a given object implements the PveHaConfig interface.
 */
export function instanceOfPveHaConfig(value: object): value is PveHaConfig {
    
    if (!('shutdownPolicy' in value) || value['shutdownPolicy'] === undefined) return false;
    
    return true;
}

export function PveHaConfigFromJSON(json: any): PveHaConfig {
    return PveHaConfigFromJSONTyped(json, false);
}

export function PveHaConfigFromJSONTyped(json: any, ignoreDiscriminator: boolean): PveHaConfig {
    if (json == null) {
        return json;
    }
    return {
        
        
        'shutdownPolicy': PveClusterOptionsShutdownPolicyEnumFromJSON(json['shutdown_policy']),
        
    };
}

export function PveHaConfigToJSON(json: any): PveHaConfig {
    return PveHaConfigToJSONTyped(json, false);
}

export function PveHaConfigToJSONTyped(value?: PveHaConfig | null, ignoreDiscriminator: boolean = false): any {
    if (value == null) {
        return value;
    }

    return {
        
        
        'shutdown_policy': PveClusterOptionsShutdownPolicyEnumToJSON(value['shutdownPolicy']),
        
    };
}

/**
 * Serialise a PveHaConfig into Proxmox's CLI-style shorthand string
 * (`key=value,…`). The property marked `x-pve-default-key` is emitted
 * positionally without a `key=` prefix; aliases collapse multiple
 * property names to the same wire key.
 *
 * Example: `{ model: 'virtio', bridge: 'vmbr0', firewall: 1 }`
 *      → `'virtio,bridge=vmbr0,firewall=1'`
 */
export function PveHaConfigToShorthand(value?: PveHaConfig | null): string | undefined {
    if (value == null) return undefined;
    const parts: string[] = [];
    
    
    if (value['shutdownPolicy'] != null) {
        parts.push(`shutdown_policy=${String(value['shutdownPolicy'])}`);
    }
    
    return parts.join(',');
}

/**
 * Parse a Proxmox shorthand string into a PveHaConfig. Mirror of
 * PveHaConfigToShorthand. The first comma-separated segment without
 * `=` is interpreted as the default-key value.
 */
export function PveHaConfigFromShorthand(input?: string | null): PveHaConfig | undefined {
    if (input == null) return undefined;
    const out: any = {};
    let defaultKeyConsumed = false;
    for (const part of input.split(',')) {
        if (!part) continue;
        const eq = part.indexOf('=');
        if (eq < 0) {
            
            continue;
        }
        const k = part.slice(0, eq);
        const v = part.slice(eq + 1);
        out[k] = v;
    }
    return out as PveHaConfig;
}

